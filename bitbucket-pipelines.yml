image: blang/latex:ubuntu

pipelines:
  default:
    - step:
        name: 'build pdfs'
        deployment: staging
        script:
          - bash install_roboto.sh
          - make all
        artifacts:
          - applications/*/*.pdf
